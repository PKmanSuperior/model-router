(*
generate finite or infinite model. there two main functions that one is call mace4 that is external finite model generating tool, and other is call internal infinite model generater.
*)
signature GENERATE =

end

structure Generate : GENERATE =
struct
end
